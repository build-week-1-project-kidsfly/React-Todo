import React from 'react';

const Todo = (props) => {
    const { groceryItem } = props;




    return (
        <div className={} onClick={}>
            <p>
                {groceryItem.name}
            </p>
        </div>
    );
};

export default Todo;
